# 📘 Weather App (날씨 앱)
> 사용자 위치 기반의 날씨 정보를 제공하고, 사용자가 원하는 위치를 저장/관리할 수 있는 안드로이드 애플리케이션

## ✨ 프로젝트 개요
| 항목 | 내용 |
| ----- | ------ |
| 🎯 목표 | 사용자 위치 정보를 관리하고, MySQL 및 Redis와 연동하여 안정적인 날씨 정보 조회 기능을 제공하는 모바일 앱 개발 |
| 🧑‍🤝‍🧑 대상 사용자 | 현재 위치 또는 관심 지역의 날씨 정보를 빠르게 확인하고 싶은 모든 사용자 |
| 📅 개발 기간 | 기말 과제 기간 |

---

## 🧑‍💻 팀 구성
| 이름 | 역할 | GITHUB | 비고 |
| ----- | ----- | ----- | ----- |
| 엄인호 |전체 개발(Front-end & Back-end) | [djsy01](https://github.com/djsy01) | 단독 작업 |

---

## 🧩 핵심 기능
- 위치 기반 날씨 조회: 현재 GPS 위치를 기반으로 실시간 날씨 정보를 조회하고 표시
- 관심 지역 등록/관리: 사용자가 원하는 도시를 저장하고 목록을 통해 쉽게 접근 가능
- 위치 순서 변경 (드래그 앤 드롭): 저장된 위치 목록의 순서를 드래그 앤 드롭으로 직관적으로 변경 및 서버에 저장
- 사용자 인증: 회원가입, 로그인, 계정 찾기 기능 (Redis 연동)
- 데이터 지속성: 사용자 위치 목록을 MySQL 데이터베이스에 저장하여 관리

---

## 🛠️ 기술 스택
| 항목 | 기술 | 상세 |
| ----- | ----- | ----- |
| Front-end (Client) | Android (Java) | GPS 기반 위치 정보 획득, UI/UX 구성, OkHttp3를 사용한 서버 통신 |
| Back-end (Server) | Node.js (Express)	| REST API 서버 구축, 데이터베이스 연동 및 비즈니스 로직 처리 |
| Database | MySQL | 사용자 등록 위치(saved_locations), 사용자 정보(users), 날씨 캐시(weather_cache) 저장 및 관리 |
| Caching/Auth | Redis | 회원가입, 로그인, 비밀번호 찾기 등의 인증 및 세션 정보 관리 (RedisManager.java 사용) |

---

## 🚧 어려웠던 점 및 해결 과정
| 문제점 | 상세 내용 | 해결 과정 |
| ----- | ----- | -----
| 위치 정보 순서 변경 (핵심) | 저장된 위치 목록의 순서를 드래그 앤 드롭으로 변경하고, 변경된 순서를 데이터베이스에 반영하는 과정에서 클라이언트-서버 연동 오류가 발생함. | 1. 클라이언트: 기존 ListView 대신 RecyclerView와 ItemTouchHelper를 도입하여 드래그 앤 드롭 기반의 UI를 구현함. 2. 백엔드: DB 테이블 이름(user_locations -> saved_locations) 불일치 문제를 해결하고, PUT /api/locations/order 엔드포인트에서 트랜잭션을 사용하여 원자성(Atomic) 있는 순서 업데이트를 구현함. |
| DB 테이블 미존재 오류 | 서버가 user_locations 테이블을 찾지 못해 모든 데이터 접근에서 ER_NO_SUCH_TABLE 오류 발생.	실제 DB에 생성된 테이블 이름(saved_locations)에 맞춰 server.js의 모든 쿼리를 수정하고, users 테이블도 추가 생성하여 외래 키 문제를 해결함. |

---

## 📂 디렉토리 구조
```
andriodstudio_finaltask/
├── app/                  # 안드로이드 클라이언트 (Front-end)
│   ├── src/
│   │   └── main/
│   │       └── java/com/cookandroid/finaltask/
│   │           ├── *.java        # 핵심 Activity 및 Manager 파일
│   │           └── LocationManageActivity.java # 위치 목록 및 순서 관리 (RecyclerView 기반)
│   └── src/main/res/layout/
│       └── activity_location_manage.xml # RecyclerView로 수정됨
├── backend/              # Node.js 서버 (Back-end)
│   └── server.js         # REST API 엔드포인트 정의 및 DB 통신 로직 (MySQL, Env 연동)
└── README.md
```